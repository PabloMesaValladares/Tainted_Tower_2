using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//[CreateAssetMenu(menuName = ("Items/Elements/Water"))]
public class Water : Elements
{
    /*public override int DamageBuff(Element rE, int dmg)
    {
        if(compareElementBuff == (int)rE && rE != Element.normal)
        {
            damageLeft = Mathf.FloorToInt(dmg * (buffPercentage / 100.0f));
        }

        return damageLeft;
    }

    public override int DamageDebuff(Element rE, int dmg)
    {
        if (compareElementDebuff == (int)rE && rE != Element.normal)
        {
            damageLeft = Mathf.FloorToInt(dmg * (debuffPercentage / 100.0f));
        }

        return damageLeft;
    }*/
}
